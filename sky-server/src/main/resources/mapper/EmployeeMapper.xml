<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
    <insert id="insert" parameterType="com.sky.entity.Employee">
        insert into
            sky_take_out.employee(name, username, password, phone, sex, id_number,
                                  create_time, update_time, create_user, update_user)
        VALUES
            (#{name}, #{username}, #{password}, #{phone}, #{sex}, #{idNumber},
             #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    
    <select id="listEmployeeByPage" parameterType="com.sky.dto.EmployeePageQueryDTO"
            resultType="com.sky.entity.Employee">
        select
            *
        from
            sky_take_out.employee e
        <where>
           <if test="name!=null and name!=''">
                and e.name = #{name}
           </if>
        </where>
    </select>
</mapper>
